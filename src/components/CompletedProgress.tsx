import { defineComponent } from 'vue'

export default defineComponent({
  name: 'CompletedProgress',
  setup() {
    return () => (
      <div>
        <h1>CompletedProgress</h1>
      </div>
    )
  },
})
